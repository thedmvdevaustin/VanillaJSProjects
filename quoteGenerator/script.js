const quote = document.querySelector(".quoteGenerator__quote");
const author = document.querySelector(".quoteGenerator__author");
const quoteBtn = document.querySelector(".quoteGenerator__button");

quoteBtn.addEventListener("click", function(){

})


//add an array of objects with a quote and author, then use a math.rand()
//to generate a random number from 0-array.length to pick the random index
//of the quote every time someone clicks the new quote button